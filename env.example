# AI Processing Server Configuration

# Server settings
HOST=0.0.0.0
PORT=8000

# Model settings
MODEL_PATH=models/yolo_weights.pt
CONFIDENCE_THRESHOLD=0.5
FRAME_INTERVAL=1.0

# Backend communication
MAIN_BACKEND_URL=http://localhost:5000
WEBHOOK_SECRET=your-webhook-secret-here

# Processing settings
MAX_VIDEO_DURATION=300
TEMP_DIR=temp

# GPU settings
GPU_ENABLED=false
DEVICE=cpu

# Optional: Database settings (for production)
# DATABASE_URL=postgresql://user:password@localhost/ai_processing 